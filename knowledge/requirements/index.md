# Requirements
* [Metadata Mutation Parity for CLI and MCP](mutation-metadata.md) - CLI and MCP creation and update must expose type, status, and tags with explicit replacement semantics and valid lifecycle values.
